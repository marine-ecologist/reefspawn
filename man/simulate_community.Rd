% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_community.R
\name{simulate_community}
\alias{simulate_community}
\title{Simulate Coral Community Composition}
\usage{
simulate_community(
  setplot,
  size,
  ratio,
  ratiovar = 0.2,
  coralcover = NULL,
  distribution = "beta",
  alpha = 12,
  beta = 4,
  seed = NULL,
  quiet = TRUE,
  plot = FALSE,
  ...
)
}
\arguments{
\item{setplot}{An \code{sf} polygon object representing the plot area (used to calculate total area).}

\item{size}{A data frame with columns \code{species} and \code{area_cm2} containing size distributions.}

\item{ratio}{A named numeric vector with proportions assigned to each growth form
(names must be: "Tabular", "Branching", "Corymbose", "Digitate", "Massive").}

\item{ratiovar}{Numeric. Proportional noise added to each growthform's ratio. Default is 0.2.}

\item{coralcover}{Numeric. Mean coral cover percentage (0–100). If \code{distribution = "beta"},
this is the mean used to sample actual coral cover.}

\item{distribution}{Character. If \code{"beta"}, coral cover is sampled from a beta distribution.
Set to \code{NULL} to use \code{coralcover} directly.}

\item{alpha}{Shape parameter α for the beta distribution (default = 12).}

\item{beta}{Shape parameter β for the beta distribution (default = 4).}

\item{seed}{Optional. An integer seed for reproducible randomization.}

\item{quiet}{verbose}

\item{plot}{Logical. If \code{TRUE}, generates diagnostic plots of the simulation and distributions.}

\item{...}{Additional arguments (currently ignored).}
}
\value{
A data frame (\code{tibble}) with columns \code{species}, \code{growthform}, \code{ratio}, \code{cover}, and \code{aerial} area per species in cm².
}
\description{
Generates a simulated coral community composition by allocating aerial cover to species
based on user-defined morphological ratios and a total coral cover estimate, optionally drawn
from a beta distribution. Optionally plots species size distributions and assigned ratios.
}
\examples{
\dontrun{
ratios <- c(Tabular = 0.2, Branching = 0.2, Corymbose = 0.2, Digitate = 0.2, Massive = 0.2)
simulate_community(setplot = my_sf_plot, size = coral_size_data, ratio = ratios, coralcover = 30)
}

}
